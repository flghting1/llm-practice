# LLM 实验记录

## 1. Prompt 对照实验

同一个问题在“随便问”“增加规则”“固定格式”三种 Prompt 下，输出的具体程度和可执行性不同。

结论：

- 角色限制回答视角；
- 规则限制任务范围；
- 输出格式提高结果稳定性；
- JSON 比自然语言更适合程序继续处理。

## 2. JSON 校验实验

使用 Python `json.load()` 验证模型输出。

验证内容：

- JSON 语法是否合法；
- 任务总时长是否为 240 分钟；
- 是否包含 learn、code、review 三种任务类型。

## 3. 幻觉实验

虚构资料不存在于当前上下文时，合格回答应明确说明无法确定，而不是自行补全。

## 4. 上下文实验

模型只能可靠使用当前对话中提供的资料。资料未提及 Docker 时，不能推断“必须使用 Docker”。

## 面试表达

LLM 是系统中的生成和推理能力模块。实际应用不能只依赖模型本身，还需要通过上下文、外部资料、结构化输出、校验和日志来保证可用性。