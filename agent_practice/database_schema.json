{
  "tables": {
    "users": {
      "description": "用户信息表",
      "columns": {
        "id": "用户编号，整数，主键",
        "name": "用户姓名，文本",
        "channel": "用户来源渠道，文本",
        "is_paid": "是否为付费用户，1 表示是，0 表示否"
      }
    },
    "products": {
      "description": "产品信息表",
      "columns": {
        "id": "产品编号，整数，主键",
        "name": "产品名称，文本",
        "category": "产品分类，文本",
        "price": "产品单价，数字"
      }
    },
    "orders": {
      "description": "订单信息表",
      "columns": {
        "id": "订单编号，整数，主键",
        "user_id": "下单用户编号，关联 users.id",
        "product_id": "产品编号，关联 products.id",
        "quantity": "购买数量，整数",
        "order_date": "下单日期，格式 YYYY-MM-DD"
      }
    }
  },
  "relationships": [
    "orders.user_id 关联 users.id",
    "orders.product_id 关联 products.id"
  ],
  "rules": [
    "只允许生成 SELECT",
    "只允许使用 users、products、orders",
    "查询结果最多返回 100 行"
  ]
}